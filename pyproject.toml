[project]
name = "mcp-server-pubtator"
version = "0.1.0"
description = "An MCP server for searching and retrieving annotations from PubTator3"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.4.1",
    "requests>=2.25.1",
]
